{
  "name": "@bid/core",
  "version": "0.1.0",
  "private": true,
  "description": "BID Trust core domain: identity, relationships, policy engine, verification engine, provider abstraction, evidence, assessment, credentials, monitoring, billing, lifecycle.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test 'test/*.test.mjs'"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
